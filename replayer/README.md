# Replays a saved match.

## Input arguments
* -f, --file : file to replay, last one will used if empty
* -d, --delay: delay in millisec between turns (default: 600)
* -i, --invert-colors: invert colors for dark terminal backgrounds
